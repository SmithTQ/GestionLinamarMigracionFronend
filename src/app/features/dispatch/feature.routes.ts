import { Routes } from '@angular/router';
import { RoutesPageComponent } from './pages/routes-page/routes-page.component';

export const DISPATCH_ROUTES: Routes = [{ path: '', component: RoutesPageComponent }];
